import { signal } from "@preact/signals";

export const alertVisible = signal<boolean>(false);
export const alertVisible2 = signal<boolean>(false);

export const ventarealizada = signal<boolean>(false);
